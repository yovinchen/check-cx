services:
  xiaofei-model-monitor:
    image: bingzi233/xiaofei-model-monitor:latest
    container_name: xiaofei-model-monitor
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
